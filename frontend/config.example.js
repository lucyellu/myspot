// Optional runtime config for static hosts such as Netlify.
// Copy to frontend/config.js in a deploy environment if you have a stable
// backend URL. For quick public sharing, start_public.bat opens Netlify with
// ?api=<cloudflare-tunnel-url>, so no checked-in config.js is required.
window.MYSPOT_API_BASE = "https://your-backend.example.com";
