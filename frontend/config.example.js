// Copy this to frontend/config.js and edit the URL to point at your backend.
// Loaded before main.js — sets the API base URL the frontend uses.
//
// Local dev: leave config.js absent or empty — same-origin (localhost:7777)
//            still works.
// Netlify:   set window.MYSPOT_API_BASE to your backend's public URL,
//            e.g. "https://myspot-api.fly.dev" or a Cloudflare tunnel URL.
window.MYSPOT_API_BASE = "";
