// Optional runtime config for static hosts such as Netlify.
// By default, api.js automatically connects to the 24/7 Oracle Cloud backend:
// https://independent-completely-precious-jewish.trycloudflare.com
// You can override it here if you run your own custom backend tunnel:
// window.MYSPOT_API_BASE = "https://your-custom-backend.trycloudflare.com";

